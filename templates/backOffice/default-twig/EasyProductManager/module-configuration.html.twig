<div class="card">
    <div class="card-header">
        <h3 class="card-title">{{ 'Easy Product Manager'|trans({}, 'easyproductmanager') }}</h3>
    </div>
    <div class="card-body">
        <p>{{ 'This module adds a dedicated product management screen accessible from the top menu.'|trans({}, 'easyproductmanager') }}</p>
        <a class="btn btn-primary" href="{{ path('easy-product-manager_list') }}">
            <i class="bi bi-box-seam"></i>
            {{ 'Manage products'|trans({}, 'easyproductmanager') }}
        </a>
    </div>
</div>
